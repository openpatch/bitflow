import { Card, CardContent } from "@openpatch/patches";
import { DocLayout } from "../../../components/DocLayout";

export const meta = {
  title: "New Task Bit",
};

To add a new task bit run `pnpm plop` and select `bit` and `task`. Afterwards
you can enter the name of the new task bit and every required file will be
generated for you. The generated files are based on the Yes No task.

Incomplete Documentation. Feel free helping to complete it. [Contributing Documentation](/docs/Contributing/documentation)

export default ({ children }) => (
  <DocLayout meta={meta}>
    <Card>
      <CardContent>{children}</CardContent>
    </Card>
  </DocLayout>
);
