node_modules
dist
# Wrangler's local Durable Object state (a room's sqlite files, written by
# `wrangler dev`) and the worker types it generates from wrangler.jsonc.
# Session rows and a 15k-line d.ts, neither of them source: `pnpm lint` in
# platforms/party regenerates the types before it type-checks.
.wrangler
worker-configuration.d.ts
.DS_Store
*.log
*.vsix
.vscode-test
# The working plan for whatever is being built next. Notes, not source:
# it is rewritten between pieces of work and says nothing about the repo as
# it stands. CLAUDE.md is the part that is meant to last.
plan.md
