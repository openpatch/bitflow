{
  "name": "bitflow-party",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./protocol": {
      "types": "./src/protocol.ts",
      "import": "./src/protocol.ts"
    }
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "build": "wrangler types && tsc --noEmit",
    "lint": "wrangler types && tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "partyserver": "^0.5.10",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@bitflow/report": "workspace:*",
    "typescript": "^7.0.2",
    "vite": "^7.3.6",
    "vitest": "^4.1.10",
    "wrangler": "^4.131.1"
  }
}
