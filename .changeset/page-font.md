---
"@bitflow/core": minor
"@bitflow/web-component": minor
---

bitflow no longer asks for Montserrat: `--bitflow-font-family` defaults to
`inherit`, so a flow is set in the font of the page it sits on. Set the token
to a font stack to give bitflow its own.
